import React, { Component } from 'react';
export default  class Resume extends Component {
  render() {
    let resumeData = this.props.resumeData;
    return (
      <section id="resume">

         <div className="row education">

            <div className="three columns header-col">
               <h1><span>Education</span></h1>
            </div>

            <div className="nine columns main-col">
              {
                resumeData.education && resumeData.education.map((item)=>{
                  return(
                    <div className="row item">
                       <div className="twelve columns">
                          <h3>{item.UniversityName}</h3>
                          <p className="info">
                          {item.specialization}
                          <span>&bull;</span> <em className="date">{item.MonthOfPassing} {item.YearOfPassing}</em></p>
                          <p>
                          {item.Achievements}
                          </p>
                       </div>
                    </div>
                  )
                })
              }
            </div>
         </div>
        <div className="row work">
            <div className="three columns header-col">
               <h1><span>Work</span></h1>
            </div>

            <div className="nine columns main-col">
              {
                resumeData.work && resumeData.work.map((item) => {
                  return(
                    <div className="row item">
                       <div className="twelve columns">
                          <h3>{item.CompanyName}</h3>
                          <p className="info">
                          {item.specialization}
                          <span>&bull;</span> <em className="date">{item.MonthOfLeaving} {item.YearOfLeaving}</em></p>
                          <p>
                          {item.Achievements}
                          </p>
                       </div>

                    </div>

                  )
                })
              }
            </div> 
         </div>


         <div className="row skill">

            <div className="three columns header-col">
               <h1><span>Skills</span></h1>
            </div>

            <div className="nine columns main-col">

               <p>
               {resumeData.skillsDescription}
               </p>

   				<div className="bars">

               <p>Command over CSS, HTML, JavaScript, JSX, JavaScript
Fundamentals +ES6, Node Package Management, Good
knowledge in backend coding, Api development
including RESTful API’s, Spring Boot microservices, Knowledge of databases like MySQL. Working
knowledge of Cloud based infrastructure services (AWS
or Azure). Command over developing React JS
components and Workflows. Understanding of
Architecture principles and patterns of DevOps.</p>
<p>Knowledge of deployment and managing services on
VM. Understanding on Site Recovery. Understanding
Load Balancer, Network Security Groups, Availability
Sets, Auto scaling. Knowledge of Virtual machines, virtual network.</p>
   				   {/* <ul className="skills">
                {
                  resumeData.skills && resumeData.skills.map((item) => {
                    return(
                      <li>
                      <span className={`bar-expand ${item.skillname.toLowerCase()}`}>
                      </span>
                      <em>{item.skillname}</em>
                      </li>
                    )
                  })
                }

   					</ul> */}

   				</div>

   			</div>

         </div>

      </section>
    );
  }
}